@using K9.SharedLibrary.Extensions
@model K9.Base.WebApplication.ViewModels.FileSourceViewModel

@if (Model.AudioFiles.Any())
{
    <br />
    @Html.Partial("_AudioFilesDisplay", Model)
}

@if (Model.VideoFiles.Any())
{
    <br />
    @Html.Partial("_VideoFilesDisplay", Model)
}

@if (Model.Images.Any())
{
    <br />
    @Html.Partial("_ImageFilesDisplay", Model)
}

@if (Model.Documents.Any())
{
    <br />
    @Html.Partial("_DocumentFilesDisplay", Model)
}
